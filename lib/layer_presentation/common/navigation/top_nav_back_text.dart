import 'package:flutter/material.dart';

class TopNavBackText extends StatelessWidget {
  const TopNavBackText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
